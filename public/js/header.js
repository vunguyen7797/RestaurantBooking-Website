

document.getElementById("checkoutbtn").addEventListener("click",  function (event) {
    document.getElementById("checkoutbtn").innerHTML = "Hello World";
        

        // try {
        //     const response = await fetch(`/login`, { "method": "GET" });
        //     if (response.status === 200)
        //        alert(response.data);
        // } catch (err) {
        //     console.error(err);
        // }
   
});